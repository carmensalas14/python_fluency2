import re

def halved(lst):
    new_lst = []
    for num in lst:
        new_lst.append(num/2)
    return new_lst
    
def only_positive(lst):
    new_lst =[]
    for num in lst
        if num > 0:
            new_lst.append(num)
    return new_lst
    
def sum(lst):
    sum = 0
    for num in lst:
        sum += num
    return sum
    
def stripped_strings(lst):
    new_lst = []
    for string in lst:
        if string = re.sub('\W', '', lst)
        string.append(new_lst)
    return new_lst
    
def find_special(lst):
    for i in range(len(lst)):
        if string == 'special'
            return i
        elif:
            return -1
            
def valid_contacts(contacts):
    new_lst =[]
    for contact in contatcs:
        if len(contact.phone_number) == 10:
            return append.(contact)
            
def contact_names(contacts):
    new_lst=[for contact in contacts return contact.name]
        
    
            
    
    